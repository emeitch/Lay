{
  "license": "MIT",
  "scripts": {
    "start": "webpack --watch -d --devtool source-map & browser-sync start --server --files build/bundle.js",
    "test": "npm run lint && cross-env NODE_ENV=test nyc --reporter=html --reporter=text mocha --compilers js:babel-register",
    "lint": "eslint src/*.js test/*.js"
  },
  "nyc": {
    "sourceMap": false,
    "instrument": false
  },
  "dependencies": {
    "bidirectional-map": "^1.0.3",
    "jssha": "^2.3.1",
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "babel-core": "^6.0.0",
    "babel-loader": "^7.1.2",
    "babel-plugin-istanbul": "^4.1.4",
    "babel-preset-env": "^1.6.1",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.24.1",
    "browser-sync": "^2.18.8",
    "cross-env": "^5.0.1",
    "eslint": "^4.0.0",
    "mocha": "^4.0.0",
    "nyc": "^11.0.2",
    "power-assert": "^1.4.2",
    "webpack": "^3.0.0"
  }
}
